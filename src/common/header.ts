let allConstants = require('../constants');
let utils = require('../utils/util');

module.exports = {
    getAllConstants () {
        return allConstants;
    },
    getAllUtils () {
        return utils;
    },
}
